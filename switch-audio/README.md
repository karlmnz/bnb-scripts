# Switch Audio

The laucher and script can be used to switch between audio outputs 
for all input streams.

Copy, revise and test the script.

Copy the launcher to your desktop (where ever) then ensure the 
launcher settings are pointing to the right path for the script.